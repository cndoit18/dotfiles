repos:
  - repo: local
    hooks:
      - id: submodule-sync
        name: Sync git submodules
        entry: git submodule update --init --recursive
        language: system
        stages: [post-merge]
        always_run: true
        pass_filenames: false
